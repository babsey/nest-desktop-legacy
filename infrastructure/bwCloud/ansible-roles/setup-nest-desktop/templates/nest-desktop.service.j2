[Unit]
Description=NEST Desktop
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/docker run -d -p 8000:8000 -p 5000:5000 -t {{ nest_desktop_image }}

[Install]
WantedBy=network.target
