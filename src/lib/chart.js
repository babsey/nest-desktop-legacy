"use strict"

module.exports = {
    barChart: require(__dirname + '/chart/bar-chart'),
    heatmapChart: require(__dirname + '/chart/heatmap-chart'),
    lineChart: require(__dirname + '/chart/line-chart'),
    scatterChart: require(__dirname + '/chart/scatter-chart')
}
